var angular = require('angular');

var module_name = 'todoServices';
var module = angular.module(module_name ,[]);

// Example of adding a possible service
// module.service(Todo.name,Todo.service);

module.exports = module_name;
